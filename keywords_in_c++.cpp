#include<iostream>
using namespace std;
int main(){
    
    cout << "C++ Reserved Keywords" << endl;
    cout << "---------------------" << endl;
    cout << "auto, int, float, while, double, char, return, if, else, switch, case, break," << endl;
    cout << "for, continue, do, sizeof, struct, union, typedef, static, const, volatile, enum, extern," << endl;
    cout << "register, short, long, signed, unsigned, namespace, using, class, public, private, protected, virtual," << endl;
    cout << "template, this, friend, inline, new, delete, try, catch, throw, operator, friend, mutable, explicit," << endl;
    cout << "export, import, module, concept, requires" << endl;
    
    return 0;
}